# Database: PostgreSQL. Use same URL for backend and Prisma.
# Copy this file to backend/.env (or run setup.bat / setup.sh which does that).
# Example local: postgresql://user:password@localhost:5432/raha_fitness
# Heroku/some hosts use postgres:// - the app normalizes it to postgresql://
DATABASE_URL=postgresql://user:password@localhost:5432/raha_fitness

# JWT secret for access tokens (change in production)
JWT_SECRET_KEY=your-secret-key-change-in-production

# Optional: Vertex AI model name (default: gemini-2.5-flash-lite)
# VERTEX_AI_MODEL=gemini-2.5-flash-lite

# AI workflow: USE_ACTION_PLANNER=true (default) uses action planner; false uses plain chat
# USE_ACTION_PLANNER=true

# AI debug: logs to backend/logs/ai_debug.csv (Excel-compatible) for testing. Set false to disable.
# AI_DEBUG_CSV=true
# AI_DEBUG_CSV_PATH=  # optional: custom path

# Website KB vector store. When using PostgreSQL, set this to use sqlite-vec for KB.
# Requires Vertex or OpenAI API key in Admin for embeddings.
# WEBSITE_KB_VEC_DB=instance/website_kb_vec.db
